# Frontend

Фронтенд базируется на `llm-council` (React + Vite). Склонируйте исходники или перенесите `frontend` из оригинального репозитория, затем выполните:

```bash
cd frontend
npm install
npm run dev
```

API ожидается на `http://localhost:8000`.
